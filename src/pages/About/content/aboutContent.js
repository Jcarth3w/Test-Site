/**
 * Hero image (optional). When you have a firm photo, either:
 * - Put the file in `public/` (e.g. `public/about-hero.jpg`) and set `src` to `/about-hero.jpg`, or
 * - Import from `src/assets/` and pass the imported URL here.
 */
export const aboutHeroImage = {
  src: null,
  alt: 'McCoy Leavitt Laskey',
};
